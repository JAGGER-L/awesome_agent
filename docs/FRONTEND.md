# Frontend

The production frontend is outside V1 scope. Runtime APIs and events must
nevertheless support a future interface that displays:

- run timeline
- agent topology and lifecycle
- per-agent conversations
- team mailbox
- dynamic task tree
- tool execution and progress
- approval requests
- artifacts and diffs
- verification and rework
- memory operations

Frontend-facing schemas must be versioned and must not expose secrets.

