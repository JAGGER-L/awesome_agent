{
  "name": "@awesome-agent/docs-site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "sync-content": "node scripts/sync-content.mjs",
    "predev": "npm run sync-content",
    "dev": "astro dev",
    "prebuild": "npm run sync-content && npm run check:navigation && npm run check:contrast",
    "build": "astro build",
    "postbuild": "node scripts/generate-llms.mjs",
    "check:links": "node scripts/check-built-links.mjs",
    "check:navigation": "node scripts/check-navigation.mjs",
    "check:contrast": "node scripts/check-contrast.mjs",
    "precheck": "npm run sync-content && npm run check:navigation && npm run check:contrast",
    "check": "astro check",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/sitemap": "3.7.3",
    "@astrojs/starlight": "0.41.4",
    "@fontsource-variable/dm-sans": "5.3.0",
    "@fontsource-variable/noto-sans-sc": "5.3.0",
    "@fontsource/dm-mono": "5.3.0",
    "astro": "7.1.3"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.6",
    "typescript": "5.9.3"
  }
}
